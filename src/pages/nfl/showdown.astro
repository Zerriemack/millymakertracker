---
const url = new URL(Astro.request.url);
const y = url.searchParams.get("year");
const dest = y ? `/nfl/${y}?view=LIST&lineupType=SHOWDOWN` : `/nfl?view=LIST&lineupType=SHOWDOWN`;
return Astro.redirect(dest, 301);
---
